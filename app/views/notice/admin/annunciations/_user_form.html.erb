<%= form_with url: { action: 'update_user' }, method: :patch do |f| %>
  <%= f.collection_select :user_tag_ids, @user_tags, :id, :name, { label: Notice::Annunciate.human_attribute_name(:user_tags) }, { multiple: true, data: { controller: 'choice' } } %>
  <%= f.submit %>
<% end %>
