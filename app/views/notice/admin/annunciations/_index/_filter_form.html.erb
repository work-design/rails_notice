<%= form_with theme: 'search', model: Notice::Annunciation.new do |f| %>
  <div class="field-body">
    <%= f.text_field :title %>
    <div class="field is-narrow">
      <%= f.submit %>
    </div>
  </div>
<% end %>
