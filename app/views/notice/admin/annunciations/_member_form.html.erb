<%= form_with url: member_admin_annunciation_path(@annunciation), method: :patch do |f| %>
  <%= f.collection_select :department_ids, @departments, :id, :name, { label: Annunciation.human_attribute_name(:user_tags) }, { multiple: true, data: { controller: 'choice' } } %>
  <%= f.submit %>
<% end %>
